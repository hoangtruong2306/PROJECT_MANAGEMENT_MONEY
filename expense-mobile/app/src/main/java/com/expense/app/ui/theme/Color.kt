package com.expense.app.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Light Mode Surfaces ──────────────────────────────────────────
val Bg100Light      = Color(0xFFF8FAFC)   // page bg
val Bg200Light      = Color(0xFFF1F5F9)   // subtle alt
val Sur100Light     = Color(0xFFFFFFFF)   // card
val Sur200Light     = Color(0xFFF8FAFC)   // card alt

// ─── Dark Mode Surfaces ───────────────────────────────────────────
val Bg100Dark       = Color(0xFF0A0F1E)   // page bg
val Bg200Dark       = Color(0xFF111827)   // surface
val Sur100Dark      = Color(0xFF1F2937)   // card
val Sur200Dark      = Color(0xFF374151)   // card alt

// ─── Primary — Navy Blue (Trust & Stability) ──────────────────────
val NavyBlue        = Color(0xFF1D4ED8)
val NavyBlueDark    = Color(0xFF60A5FA)
val NavySurface     = Color(0xFFEFF6FF)
val NavySurfaceDark = Color(0xFF1E3A8A)

// ─── Income — Emerald Green ───────────────────────────────────────
val Emerald         = Color(0xFF059669)
val EmeraldLight    = Color(0xFF34D399)
val EmeraldSurface  = Color(0xFFD1FAE5)
val EmeraldSurfDark = Color(0xFF064E3B)

// ─── Expense — Rose Red ───────────────────────────────────────────
val Rose            = Color(0xFFDC2626)
val RoseLight       = Color(0xFFF87171)
val RoseSurface     = Color(0xFFFEE2E2)
val RoseSurfDark    = Color(0xFF7F1D1D)

// ─── Warning — Amber ─────────────────────────────────────────────
val Amber           = Color(0xFFD97706)
val AmberLight      = Color(0xFFFBBF24)
val AmberSurface    = Color(0xFFFEF3C7)
val AmberSurfDark   = Color(0xFF78350F)

// ─── Text — Light ─────────────────────────────────────────────────
val TxtPrimaryL     = Color(0xFF0F172A)
val TxtSecondaryL   = Color(0xFF475569)
val TxtMutedL       = Color(0xFF94A3B8)
val BorderL         = Color(0xFFE2E8F0)

// ─── Text — Dark ──────────────────────────────────────────────────
val TxtPrimaryD     = Color(0xFFF9FAFB)
val TxtSecondaryD   = Color(0xFF9CA3AF)
val TxtMutedD       = Color(0xFF6B7280)
val BorderD         = Color(0xFF374151)

// ─── Backward-compat aliases (used across screens) ────────────────
val PrimaryGreen        = Emerald
val PrimaryGreenLight   = EmeraldLight
val PrimaryGreenDark    = Color(0xFF047857)
val PrimaryGreenSurface = EmeraldSurface
val BackgroundSlate     = Bg100Light
val SurfaceWhite        = Sur100Light
val TextPrimary         = TxtPrimaryL
val TextSecondary       = TxtSecondaryL
val TextMuted           = TxtMutedL
val BorderGray          = BorderL
val DangerRose          = Rose
val DangerLight         = RoseSurface
val WarningAmber        = Amber
val WarningLight        = AmberSurface
val SuccessMint         = Color(0xFF10B981)
val SuccessLight        = EmeraldSurface
val AccentTeal          = Color(0xFF0D9488)
val AccentCyan          = Color(0xFF06B6D4)
val PrimaryIndigo       = Color(0xFF4F46E5)
val PrimaryLight        = Color(0xFFEEF2FF)
val GradientDarkStart   = Color(0xFF064E3B)
val GradientGreenMid    = Color(0xFF065F46)
val GradientGreenStart  = Emerald
val NavyGradientStart   = Color(0xFF0F172A)
val NavyGradientEnd     = Color(0xFF1E293B)
